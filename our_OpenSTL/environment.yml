name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - xarray==0.19.0
  - pip:
    - scikit-image
    - timm
    - tqdm
    - opencv-python
    - wandb
prefix: /opt/anaconda3/envs/simvp